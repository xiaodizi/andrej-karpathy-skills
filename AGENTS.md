# AGENTS.md

## Repo focus
- This repo is documentation/guidelines only; no build/test commands or runtime entrypoints are defined in the root.

## Source-of-truth files
- Behavioral guidelines live in `CLAUDE.md` and the Cursor rule `.cursor/rules/karpathy-guidelines.mdc`.
- If you change the four principles, keep `CLAUDE.md` and `.cursor/rules/karpathy-guidelines.mdc` in sync (see `CURSOR.md`).

## Tooling notes
- Cursor users rely on the committed `.cursor/rules/karpathy-guidelines.mdc` (`alwaysApply: true`), not `CLAUDE.md`.
